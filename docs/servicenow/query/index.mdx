---
description:
    Server-side namespace for performing arbitrary GlideRecordSecure queries,
    Should not typically be invoked directly. This script primarily exists to
    adapt queries from the QueryAjax utility to a standard GlideRecordSecure
    query. Records are returned as plain JavaScript objects containing only the
    specified fields.
requires:
    - ServiceNow/Check
env:
    api: Query
    platform: ServiceNow
    runtime: Server
    resource: Script Include
tags: [api, servicenow, server, query]
---

import Readme from '@components/TypeDocReadme.astro';

<Readme platform={frontmatter.env.platform} api={frontmatter.env.api} />
