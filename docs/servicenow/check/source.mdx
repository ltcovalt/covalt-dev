---
description:
    Input validation and error handling library that that uses a fluent,
    chainable API. Leverages a standard JavaScript class instead of
    Prototype.js.
requires:
    - Type
env:
    api: Check
    platform: ServiceNow
    runtime: Server
    resource: Script Include
tags: [api, servicenow, server, type, input, validation, class, es6, es12]
---

```js file=<rootDir>/docs/servicenow/check/Check.js

```
