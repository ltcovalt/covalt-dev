---
description:
    UI script wrapper for the QueryAjax/Query script includes. Provides a
    simplified interface for interacting with the Query namespaces, response
    parsing, and error handling.

requires:
    - ServiceNow/QueryAjax
env:
    api: Query
    platform: ServiceNow
    runtime: Client
    resource: UI Script
tags: [api, servicenow, server, query, ajax, ui script]
---

import Readme from '@components/TypeDocReadme.astro';

<Readme platform={frontmatter.env.platform} api={'query-client'} />
