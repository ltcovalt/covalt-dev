[Query](../README.md) / JsendStatus

# Type Alias: JsendStatus

> **JsendStatus**<> = `"success"` | `"fail"` | `"error"`

JSEND formatted response status

* `'success'` — request completed successfully
* `'fail'` — an expected client-side validation error
* `'error'` — unexpected server-side failure

## Type Parameters

| Type Parameter |
| ------ |
