---
description:
    Server-side namespace containing methods for obtaining detailed type info
    and performing type checks on complex object types
env:
    api: Type
    platform: ServiceNow
    runtime: Server
    resource: Script Include
tags:
    [
        api,
        servicenow,
        server,
        namespace,
        type,
        assert,
        script include,
        validation,
        facade,
    ]
---
import Readme from '@components/TypeDocReadme.astro';

<Readme platform={frontmatter.env.platform} api={frontmatter.env.api} />
