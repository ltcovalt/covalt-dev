---
import InfoIcon from './InfoIcon.svg';
import AnnounceIcon from './AnnounceIcon.svg';
import WarningIcon from './WarningIcon.svg';
import ErrorIcon from './ErrorIcon.svg';
import CriticalIcon from './CriticalIcon.svg';

const { sev='info', type="minimal", title=sev } = Astro.props;
const iconMap = {
	'announcement': AnnounceIcon,
	'info': InfoIcon,
	'caution': WarningIcon,
	'warning': WarningIcon,
	'error': ErrorIcon,
	'critical': CriticalIcon
};
const Icon = iconMap[sev] ?? InfoIcon;
const styles = {
	solid: ''
}
---
<div class={`callout callout-${sev} callout-${type}`}>
	{
		() => {
			if (!title) return;
			return (
				<span class="font-extrabold flex items-center gap-2 callout-title">
					<Icon class="size-5 inline-block align-middle relative top-px"/>
					{ title }
				</span> 
			);
		}
	}
	<slot />
</div>